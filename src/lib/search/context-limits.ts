/** Default hybrid retrieval count for project chat. */
export const PROJECT_RETRIEVAL_LIMIT = 24;

/** Default hybrid retrieval count for global search. */
export const SEARCH_RETRIEVAL_LIMIT = 28;

/** Chunks passed into answer/generation prompts after ranking. */
export const ANSWER_CONTEXT_CHUNK_LIMIT = 22;
